import React from 'react';
import { connect } from 'react-redux';

const StageThree = ( props, context ) => {
  debugger;
  return (
    <div>
      Stage Three
    </div>
  )
};

export default connect()(StageThree);